#create a virtual environement to use Python, should be a the root of the project
python3 -m venv ./venv
#activate the virtual to use pyhton cmd
source ./venv/bin/activate
#close virtualisation
deactivate

#install Django
pip3 install django
pip install --upgrade pip

#run server from manage.py location
python manage.py runserver
